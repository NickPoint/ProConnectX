/*
import {createAppSlice} from "../../app/createAppSlice";

export interface HeaderSliceState {
    name: string | null;
    avatar: string | null;
}

const initialState: HeaderSliceState = {
    name: null,
    avatar: null,
}

const headerSlice = createAppSlice({
    name: "header",
    initialState,
    reducers: {
        set
    },
    selectors: {
        selectAddButtonVisible: state => state.addButtonVisible,
    },
});*/
